# 要件定義書：クロスデバイス対応メモ帳アプリ

## 1. プロジェクト概要
- **名称**：クロスデバイス対応メモ帳アプリ  
- **目的**：PC／スマホ／タブレットなど、複数デバイスからメモの作成・閲覧・更新・削除を行えるシンプルなWebアプリケーションを提供する。

## 2. 用語定義
| 用語       | 意味                                 |
| ---------- | ------------------------------------ |
| メモ       | ユーザーが作成するテキストデータ     |
| ホーム画面 | ユーザーのメモ一覧を表示する画面     |
| メモ画面   | 個別メモの詳細表示・編集・削除を行う画面 |

## 3. 機能要件

### 3.1 ユーザー機能
1. **メモ作成**  
   - 「新規作成」ボタンからタイトル・内容を入力し保存できる。  
2. **メモ閲覧**  
   - ホーム画面で一覧を表示し、クリックで詳細画面に遷移できる。  
3. **メモ編集**  
   - 詳細画面の `<textarea>` で内容を編集し、「更新」ボタンで反映できる。  
4. **メモ削除**  
   - 詳細画面の「削除」ボタンを押すことで該当メモを削除できる。  

### 3.2 管理者／システム機能
- 本フェーズではユーザー認証や管理者機能は対象外

## 4. 画面要件

### 4.1 ホーム画面 (`GET /`)
- **表示内容**  
  - ユーザーが作成したメモ一覧（タイトル・作成日時・更新日時）  
- **操作**  
  - メモタイトルをクリック → `/memo/:id` へ遷移  
  - 「新規作成」ボタン → `/memo/new`（またはモーダル）へ遷移  

### 4.2 メモ詳細・編集画面 (`GET /memo/:id`)
- **表示内容**  
  - タイトル（テキスト）  
  - 本文（`<textarea>`内に表示）  
- **操作**  
  - 本文編集後、「更新（PATCH）」ボタンで変更を反映  
  - 「削除（DELETE）」ボタンでメモを削除  
  - 「戻る」ボタンでホーム画面へ戻る  

### 4.3 メモ新規作成画面 (`GET /memo/new`)
- **表示内容・操作**  
  - タイトル入力欄 ＆ 本文 `<textarea>`  
  - 「作成（POST）」ボタンで新規登録  
  - 「キャンセル」ボタンでホーム画面へ戻る  

## 5. システム要件

### 5.1 技術要件
- **フレームワーク**：Ruby on Rails（7.x）  
- **ビュー**：ERBテンプレート  
- **CSS**：Bootstrap v5.x  
- **DB**：SQLite（開発環境）  
- **その他**：Ruby 3.x、CSRF保護、入力サニタイズ

### 5.2 ルーティング／エンドポイント
| HTTPメソッド | パス            | アクション              | コントローラ#アクション |
| ------------ | --------------- | ----------------------- | ----------------------- |
| GET          | `/`             | メモ一覧表示            | `memos#index`          |
| GET          | `/memo/new`     | 新規作成フォーム表示    | `memos#new`            |
| POST         | `/memo`         | メモ新規登録            | `memos#create`         |
| GET          | `/memo/:id`     | メモ詳細・編集フォーム  | `memos#show`           |
| PATCH        | `/memo/:id`     | メモ更新                | `memos#update`         |
| DELETE       | `/memo/:id`     | メモ削除                | `memos#destroy`        |

### 5.3 非機能要件
- **レスポンシブ対応**：Bootstrapグリッド利用  
- **パフォーマンス**：想定利用量は少数のため標準構成で可  
- **拡張性**：将来的な認証・タグ付け・検索・API公開を想定

## 6. データベース設計

### テーブル：`memos`
| カラム名     | 型       | 制約         | 説明                   |
| ------------ | -------- | ------------ | ---------------------- |
| id           | integer  | PK, auto     | 自動採番               |
| title        | string   | NOT NULL     | メモのタイトル         |
| body         | text     | NOT NULL     | メモ本文               |
| created_at   | datetime |              | 作成日時（自動）       |
| updated_at   | datetime |              | 更新日時（自動）       |

## 7. 考慮事項
- **新規作成画面**は詳細画面とUIは共通化可能だが、`new`/`create`でアクションを分離  
- **将来的拡張**  
  - ユーザー認証機能  
  - メモのタグ付け・検索  
  - JSON API提供  

